one_hot_encodding()
  one-hot encodding functions are applied on the dataset
	hint:
		 outputs :
			 -> signals: numpy array that includes 12-lead ECG signal.
			 -> labels : one-hot encodded numpy array of diagnosed labels.
			 -> The signals with this indices have no label, so they are romoved:
				[50]


----------------------------------------------------------------